[tool.uv.workspace]
members = ["packages/*"]

[tool.uv]
dev-dependencies = [
    "pytest>=7.0.0",
    "pytest-asyncio>=0.23.0",
    "ruff>=0.13.1",
    "ty>=0.0.1a21",
    "pre-commit>=3.5.0"
]
override-dependencies = ["prime-tunnel>=0.1.6"]

[tool.uv.sources]
prime-tunnel = { workspace = true }
verifiers = { git = "https://github.com/PrimeIntellect-ai/verifiers.git", tag = "v0.1.12.dev6" }

[tool.pytest.ini_options]
pythonpath = [
    "packages/prime/src",
    "packages/prime-sandboxes/src",
    "packages/prime-evals/src",
    "packages/prime-mcp-server/src",
]
testpaths = [
    "packages/*/tests",
]
